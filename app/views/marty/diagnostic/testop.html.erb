<!DOCTYPE html>
<html>
<head>
  <title>Gemini Diagnostics</title>
  <% if @show_detail %>
  <style type="text/css">
    body { margin: 0;
           padding: 0;
           border: 0;
           background-color: #fff;
           color: #333333;
           text-align: left;
           font-family: Arial, Helvetica, Verdana, sans-serif;
           font-weight: normal;
           font-size: 11pt; }
    table { border-width: 0px;
            margin-left: auto;
            margin-right: auto;
            border-spacing: 1px;
            width: 75% }
    th { padding: 9px;
         background-color: #d6d6d6 }
    td { padding: 9px;
         -moz-border-radius: 5px;
         border-radius: 5px;
         -webkit-border-radius: 5px;
         border-style: solid;
         border-width: 1px;
         border-color: #999999; }
    tr.passed { background-color: #9ed515 }
    tr.warning { background-color: #ffdb58 }
    tr.failed { background-color: #ff5555 }
    td.desc { font-size: 10pt; }
    h1 { display: block;
         margin: 0px auto 40px auto;
         padding: 8px;
         background-color: #3e2155;
         font-size: 18pt;
         color: #ffffff;
         line-height: 1.5em; }
  </style>
  <% end %>
</head>
<body>
  <div>
    <% if @show_detail %>
      <h1>Gemini Diagnostics</h1>
      <table>
        <th width=20%>Diagnostic Name</th>
        <th width=10%>Status</th>
        <th width=70%>details</th>
        <% @result.each do |d| %>
        <tr class="<%= d.status_text.downcase %>">
          <td><%= d.name %></td>
          <td><%= d.status_text %></td>
          <td class="desc"><%= d.details %></td>
        </tr>
        <% end %>
      </table>
    <% else %>
      <%= @data %>
    <% end %>
  </div>
</body>
</html>
